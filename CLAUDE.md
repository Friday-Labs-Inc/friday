# friday

## Agent skills

Both `CLAUDE.md` and `AGENTS.md` carry this block, identically. They are thin
pointers: the substance lives in `docs/agents/`, which is single-sourced, so
the two files cannot drift on anything that matters. Edit `docs/agents/*.md`,
not this block.

### Issue tracker

GitHub Issues, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical labels, unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the root, ADRs in `docs/adr/` — note this repo also has an older `docs/decisions/`. See `docs/agents/domain.md`.
